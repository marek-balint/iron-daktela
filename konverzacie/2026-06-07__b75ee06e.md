# Konverzácia — 2026-06-07

- **Session:** `b75ee06e`
- **Od:** 2026-06-07 21:55 — **do:** 2026-06-07 22:27
- **Vetva:** `HEAD`
- **Počet správ:** 6

> Tool výstupy a interné „thinking“ sú vynechané pre čitateľnosť — kompletný záznam je v `raw/`.

---

## 🧑 Marek  ·  _2026-06-07 21:55_

to .md add something about security, and be carefoul on that.. sql ijnection, xss, etc.. and in cinfiguraton in prestashop add manual setting on score with ai

## 🤖 Claude  ·  _2026-06-07 21:55_

I'll start by exploring the existing documentation structure to understand the project layout and conventions before making changes.

- 💻 Bash: `ls -la .claude/docs/ && echo "---" && ls -la`  — _List docs and root directory_

Let me read the existing docs to match style and conventions.

- 📖 Read `.claude/docs/02-architecture.md`

- 📖 Read `.claude/docs/05-prioritization.md`

- 📖 Read `.claude/docs/04-ai-classification.md`

- 📖 Read `.claude/docs/03-daktela-integration.md`

Now I understand the project well. Let me create a dedicated security doc and wire in the manual-score-with-AI configuration setting. First, the security doc:

- 📝 Write `.claude/docs/07-security.md`

Now the manual-score-with-AI configuration. Let me add a dedicated section to the prioritization doc:

- ✏️ Edit `.claude/docs/05-prioritization.md`

Now let me update the architecture doc's config section to list the new manual-score setting, and add a `Security` note to the components:

- ✏️ Edit `.claude/docs/02-architecture.md`

Now update the CLAUDE.md documentation index to add the security doc:

- ✏️ Edit `.claude/CLAUDE.md`

Let me add a security item to the hard constraints in CLAUDE.md, since the user emphasized caring about it:

- ✏️ Edit `.claude/CLAUDE.md`

Done. Here's what I added:

## New: `docs/07-security.md`
A dedicated security doc tailored to this module's real attack surface — untrusted customer email text flowing into SQL, an HTML admin page, and an LLM prompt:

1. **Threat model** table mapping assets → threats → defences.
2. **SQL injection** — never interpolate; `pSQL()`/`(int)`/`bqSQL()`, allow-listed `ORDER BY`, with BAD vs GOOD code. Flags the enrichment service since it builds catalog lookups from AI-extracted `key_phrases` (still untrusted).
3. **XSS** — escape on output in templates, no `|raw`/`nofilter`; treat AI `summary`/`suggested_answer` as untrusted HTML too; sanitize email bodies.
4. **Secrets** — config/`.env` only, never logged/committed, redact, no stack traces to browser.
5. **Access control & CSRF** — extend `ModuleAdminController` for auth + permission + token; keep `cli/sync.php` off HTTP so nobody can burn API budget.
6. **Prompt injection** — closed enums, validate every field, low-impact-by-design.
7. **PII minimization** + **transport/deps** (HTTPS, `composer audit`).
8. A **per-PR security checklist**.

## Manual score setting (config in PrestaShop)
In `docs/05-prioritization.md` I added a **"Manual score override"** section:
- A module config key `DAKTELA_SCORE_MODE` with three modes: `ai` (default, auto), `manual` (human enters the priority number), `ai_assisted` (AI default, agent can adjust per ticket).
- **Per-ticket** `manual_score` (0–100) stored with `manual_score_by`/`manual_score_at`; effective score = manual if set else AI score, both stored so ranking stays explainable. Setting it is flagged as a CSRF/permission-protected, range-validated admin action.

## Wiring
- `02-architecture.md` — listed `DAKTELA_SCORE_MODE` in config + a secrets-security note.
- `CLAUDE.md` — added doc 07 to the index, updated doc 05's description, and added hard constraint **#6 "Security is not optional."**

One thing I noticed but left alone: `02-architecture.md:47` has a leftover Czech TODO (`- dokoncit`) in the enrichment note — want me to clean that up too?

## 🧑 Marek  ·  _2026-06-07 22:15_

in 04-ai .md in Where the draft lives add this to to do in md and remove all dev files/words

in 04-ai .md in Where the draft lives add second one to do in md and remove all dev files/words

## 🤖 Claude  ·  _2026-06-07 22:17_

Let me re-read the exact "Where the draft lives" section to edit it precisely.

- 📖 Read `.claude/docs/04-ai-classification.md`

- ✏️ Edit `.claude/docs/04-ai-classification.md`

Done. In the **"Where the draft lives"** section of `04-ai-classification.md`:

- **Converted the two options into a markdown to-do list** — the always-do storage is `- [x]`, and the second option (push drafts back into Daktela) is now `- [ ]`, an open to-do.
- **Removed all dev/code words**: the table/column names (`ps_daktela_ticket`, `suggested_answer` column, `ps_daktela_draft` table) and the code-identifier metadata list (`model_used`, `answer_grounded`, `products_referenced`, `generated_at`, `agent_action`, `sent | edited | discarded`) — all rewritten as plain prose ("our own module storage", "which model produced it", "sent, edited, or discarded", etc.).

## 🧑 Marek  ·  _2026-06-07 22:21_

and in the prestashop ui view in administration where is configuration and customer -> daktela add some tooltips how functions are working, en/sk

## 🤖 Claude  ·  _2026-06-07 22:21_

- 📖 Read `.claude/docs/02-architecture.md`

I'll expand the Presentation section in doc 02 to describe both admin views and document the bilingual tooltip copy.

- ✏️ Edit `.claude/docs/02-architecture.md`

Let me update the doc 02 index entry in CLAUDE.md to reflect the new UI/tooltips content:

- ✏️ Edit `.claude/CLAUDE.md`

Done. In `docs/02-architecture.md` I expanded the **Presentation** section and added a new **"Admin UI tooltips (en / sk)"** section covering both back-office screens you named:

- **Configuration page** — tooltips for Daktela base URL, access token, Anthropic API key, Anthropic model, **Score mode** (ai/manual/ai-assisted), and scoring weights.
- **Customer → Daktela view** — tooltips for priority score, category, sentiment, urgency, customer value, waiting/SLA, manual score override, and suggested draft.

Each is a one-sentence help bubble in **two columns: EN and SK**. I also documented *how* they should be wired — through PrestaShop's i18n (`$this->trans(...)`, domain `Modules.Daktela.Admin`) so the `en`/`sk` strings come from translation files rather than being hard-coded, and added a security note that tooltips never expose secrets (token/key fields stay masked, write-only) cross-referencing `docs/07-security.md`.

Updated the doc index in `CLAUDE.md` to mention the tooltips.

One note: I used standard Slovak (sk) for the translations. If your shop actually targets **Czech (cs)** — doc 04's schema lists `cs` as a language and the support addresses use `reklamace@` (Czech) — let me know and I'll swap or add a `cs` column.
